PORT=4317

# Optional. The UI can also provide these at runtime.
LLM_ENDPOINT=
LLM_API_KEY=
LLM_MODEL=

# Optional default Seedance settings. Provider payloads vary, so the UI also has
# a request-template editor.
SEEDANCE_ENDPOINT=
SEEDANCE_API_KEY=
SEEDANCE_MODEL=
